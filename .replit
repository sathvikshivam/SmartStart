run = "php -S 0.0.0.0:8000 -t ."

entrypoint = "index.html"
modules = ["php-8.2"]

[nix]
channel = "stable-22_11"

[deployment]
run = ["php", "-S", "0.0.0.0:8000", "-t", "."]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8000
externalPort = 80
